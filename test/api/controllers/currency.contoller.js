const should = require('should');
const request = require('supertest');
const server = require('../../../app');

describe('controllers', () => {
  describe('curreny', () => {
    describe('GET /coin-info', () => {
      it('should return a coin info', done => {
        done();
      });

      it('should accept a pair parameter', done => {
        done();
      });
    });
  });
});
